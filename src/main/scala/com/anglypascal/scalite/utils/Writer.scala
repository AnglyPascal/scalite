package com.anglypascal.scalite.writers

/** write the files that the engine is rendering, and copy the files that are
  * not being modified.
  *
  * also write cache to _cache directory and metadata to _metadata directory
  */

object Writer
